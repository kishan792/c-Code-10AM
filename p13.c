//program to find the sum of the given two numbers . 
#include<stdio.h>
void main(){
    int a, b, sum;
    printf("enter a and b ");
    scanf("%d%d",&a,&b);
    sum = a+b; 
    printf("sum pf the given two number is : %d", sum );
}